# タグページ自動生成スクリプト

**Status**: ✅ Phase 3.1 完了（v2.0.0）  
**最終更新**: 2025-12-22

---

## 🎉 Phase 1 完了

**実装完了日**: 2025-12-21

### 達成した機能

✅ **自動タグ検出**: 全日別HTMLから`data-tags`属性を自動抽出  
✅ **静的HTML生成**: JavaScriptなしで高速表示  
✅ **統計情報**: 件数、最新/最古日付を自動表示  
✅ **柔軟なフォーマット**: `timeline=100`, `music` どちらも対応  
✅ **コマンドライン**: タグフィルター、デバッグモード搭載  

### 生成されたページ

- `txt/zakki/tag/timeline.html` - 9セクション
- `txt/zakki/tag/music.html` - 8セクション

### パフォーマンス

- 処理ファイル: 22ファイル
- 検出セクション: 17セクション
- 実行時間: 約1秒

---

## 🎉 Phase 2 完了

**実装完了日**: 2025-12-21

### 達成した機能

✅ **関連度ソート**: `--sort-by`オプションで4種類のソート対応  
✅ **平均関連度表示**: 各タグの平均関連度を自動計算・表示  
✅ **複数ソート順**: date-desc/asc, relevance-desc/asc に対応  
✅ **タグ一覧ページ**: `tag_main.html`を自動生成  

### 追加されたページ

- `txt/zakki/tag/tag_main.html` - 全タグの一覧ページ
  - 件数順でソート
  - 日付範囲と平均関連度を表示
  - 各タグへのリンク

### ソートオプション

```bash
--sort-by date-desc       # 日付降順（デフォルト）
--sort-by date-asc        # 日付昇順  
--sort-by relevance-desc  # 関連度降順
--sort-by relevance-asc   # 関連度昇順
```

---

## 概要

日別の雑記HTMLファイルから`data-tags`属性を読み込んで、タグごとの統合ページを自動生成するPythonスクリプトです。
`build_month.py`と同様のアプローチで、静的HTMLを事前生成することで高速表示とSEO最適化を実現します。

## 目的と背景

### 現在の仕組み

1. **日別HTML**: `data-tags="timeline=100,music=80"` 形式でセクションにタグ付け
2. **タグページHTML**: `timeline.html`, `music.html` などの個別ページ
3. **tag-loader.js**: クライアント側で全日別HTMLを動的に読み込み、タグでフィルタリング

### 問題点

| 問題 | 詳細 |
|------|------|
| **パフォーマンス** | ブラウザで全期間（2024〜2026）の記事を毎回読み込む必要があり、重い |
| **SEO** | JavaScriptで動的にレンダリングされるため、検索エンジンにインデックスされにくい |
| **手動作成** | 新しいタグページを手動で作る必要がある（タグ名を変更） |
| **初期表示** | ローディング時間が長く、ユーザー体験が低下 |

### 解決策

**静的HTMLを事前生成**することで、これらの問題をすべて解決します。

## 設計方針

### Python版の利点（build_month.pyと同様）

1. ✅ **静的HTML生成**: ビルド時に全タグページを生成
2. ✅ **高速表示**: JavaScriptなしで即座に表示
3. ✅ **SEO最適化**: 静的コンテンツで検索エンジンに完全インデックス
4. ✅ **自動検出**: 全`data-tags`を走査して自動生成（手動不要）
5. ✅ **一貫性**: `build_month.py`と同じ設計思想・コード構造

## スクリプト仕様

### ファイル名

```
scripts/build_tags.py
```

### 基本的な使い方

```bash
# プロジェクトルートから実行（推奨）
python ./scripts/build_tags.py

# scriptsディレクトリから実行
cd scripts
python build_tags.py

# 特定のタグのみ生成
python ./scripts/build_tags.py timeline music

# 関連度降順でソート
python ./scripts/build_tags.py --sort-by relevance-desc

# 日付昇順でソート
python ./scripts/build_tags.py --sort-by date-asc

# zakki ルートディレクトリを明示的に指定（通常不要）
python ./scripts/build_tags.py --zakki-root D:\web\100percent-health\txt\zakki

# デバッグモード
python ./scripts/build_tags.py --debug
```

**注意**: スクリプトはプロジェクトルートまたはscriptsディレクトリから実行できます。パスは自動的に解決されます。

### コマンドライン引数

| 引数 | 説明 | デフォルト | 例 |
|------|------|-----------|-----|
| `tags` | 生成するタグ名（省略時は全タグ） | 全タグ | `timeline music` |
| `--zakki-root` | zakki ディレクトリのパス | `{project}/txt/zakki` | `./txt/zakki` |
| `--output-dir` | 出力先ディレクトリ | `{zakki-root}/tag` | `./txt/zakki/tag` |
| `--sort-by` | ソート方法（v1.1.0〜） | `date-desc` | `date-desc`, `date-asc`, `relevance-desc`, `relevance-asc` |
| `--config` | 設定ファイルのパス（v2.0.0〜） | 自動検出 | `build_tags_config.yaml` |
| `--debug` | デバッグ情報を表示 | `False` | - |

**注**: `--zakki-root`のデフォルトはスクリプトファイルの位置から自動的に計算されます。通常は指定不要です。

### 設定ファイル（v2.0.0〜）

**設定ファイルの自動検出**:
スクリプトは以下の順序で設定ファイルを自動検出します：
1. `scripts/build_tags_config.yaml`
2. `scripts/build_tags_config.json`
3. `--config` オプションで指定されたパス

**設定ファイルの例（YAML形式）**:

```yaml
# scripts/build_tags_config.yaml

# グローバル設定
zakki_root: "../txt/zakki"
output_dir: "../txt/zakki/tag"
default_sort: "date-desc"

# txt_main.html の自動更新設定
txt_main_path: "../txt/txt_main.html"
update_txt_main: true
txt_main_tag_sort: "count-desc"  # count-desc, count-asc, name-asc, name-desc

# タグ別設定
tags:
  timeline:
    sort: "date-desc"
    description: "個人史・活動のタイムライン"
  music:
    sort: "relevance-desc"
    description: "音楽に関する記事"
```

**設定ファイルの例（JSON形式）**:

```json
{
  "zakki_root": "../txt/zakki",
  "output_dir": "../txt/zakki/tag",
  "default_sort": "date-desc",
  "txt_main_path": "../txt/txt_main.html",
  "update_txt_main": true,
  "txt_main_tag_sort": "count-desc",
  "tags": {
    "timeline": {
      "sort": "date-desc",
      "description": "個人史・活動のタイムライン"
    },
    "music": {
      "sort": "relevance-desc",
      "description": "音楽に関する記事"
    }
  }
}
```

**優先順位**:
1. コマンドライン引数（最優先）
2. 設定ファイル
3. デフォルト値

**例**:
```bash
# 設定ファイルを使用（自動検出）
python build_tags.py

# 設定ファイルでmusic, timelineのソート順を指定しつつ、
# コマンドラインでdebugモードを有効化
python build_tags.py --debug

# 設定ファイルを無視してコマンドラインで全指定
python build_tags.py --sort-by relevance-desc --zakki-root ./txt/zakki
```

### 出力先

- **入力**: `txt/zakki/YYYY/MM/days/*.html` 内の全HTMLファイル
- **出力**: `txt/zakki/tag/{tagname}.html`（タグごと）

## 処理フロー

```
1. zakki ルートディレクトリを走査
   ├─ 2024/07/days/*.html
   ├─ 2024/08/days/*.html
   ├─ ...
   └─ 2026/01/days/*.html

2. 各日別HTMLから data-tags 属性を抽出
   例: <section data-tags="timeline=100,music=80">
   → { "timeline": 100, "music": 80 }

3. タグごとにセクションを収集
   timeline: [
     { date: "2024-12-13", section: "<section>...</section>", relevance: 100 },
     { date: "2025-01-01", section: "<section>...</section>", relevance: 90 },
     ...
   ]

4. 各タグについて統計情報を計算
   - 記事数
   - 最新日付
   - 最古日付
   - 平均関連度

5. タグごとにHTMLテンプレートを生成
   - 日付順でソート（デフォルト: 新しい順）
   - セクションをそのまま埋め込み
   - ナビゲーションリンクを追加

6. txt/zakki/tag/{tagname}.html に書き出し

7. tag_main.html（タグ一覧）を生成

8. txt/txt_main.html の #taglist を更新（オプション）
   - 正規表現で該当部分のみを置換
   - 他の部分のインデントを保持
```

## 出力構造

### ファイル配置

```
txt/zakki/tag/
├── timeline.html        # タグページ（静的HTML）
├── music.html           # タグページ（静的HTML）
├── anime.html           # （新しいタグも自動追加）
├── diary.html           # （自動検出されたタグ）
├── tag_main.html        # タグ一覧ページ（将来実装）
├── tag-loader.js        # 既存（後方互換のため残す）
└── tag-controls.css     # 既存のまま
```

### 生成されるHTML構造

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>tag: timeline - 100%health</title>
  <!-- 既存のスタイルシート -->
</head>
<body>
  <div id="wrapper">
    <header id="header">
      <!-- パンくずナビゲーション -->
    </header>
    
    <main id="main">
      <h1>timeline タグ一覧</h1>
      
      <!-- タグ統計情報 -->
      <div class="tag-stats">
        <p>全9件 | 最新: 2025-12-08 | 最古: 2024-12-13 | 平均関連度: 99%</p>
      </div>
      
      <!-- タグ付きセクション一覧 -->
      <div class="tag-articles">
        <article class="tag-article" data-date="2025-12-08">
          <h2><a href="/txt/zakki/2025/12/days/2025-12-08.html">2025-12-08</a></h2>
          <section data-tags="timeline=100">
            <h4>まとめの表</h4>
            <ul class="timeline_md">
              <li>08月27日 100%health:雑記ページ更新&諸々更新</li>
              <li>08月29日 いど子通信#43@twitch</li>
              ...
            </ul>
          </section>
        </article>
        
        <article class="tag-article" data-date="2024-12-13">
          <h2><a href="/txt/zakki/2024/12/days/2024-12-13.html">2024-12-13</a></h2>
          <section data-tags="timeline=100">
            <h4>まとめの表</h4>
            <ul class="timeline_md">
              <li>10月15日 いど子通信#9</li>
              ...
            </ul>
          </section>
        </article>
      </div>
    </main>
    
    <footer id="main-footer">
      <!-- フッター -->
    </footer>
  </div>
</body>
</html>
```

## 実装段階

### Phase 1: 基本機能（MVP）✅ 完了

**目標**: 最小限の機能で動作するスクリプトを作成

- ✅ 全日別HTMLの走査
- ✅ `data-tags`属性の抽出とパース
- ✅ タグごとにセクションを収集
- ✅ 基本的なHTMLテンプレート生成
- ✅ 日付順ソート（降順: 新しい順）
- ✅ ファイルへの書き出し
- ✅ タグ統計情報の表示（件数、最新/最古日付）
- ✅ コマンドライン引数対応（タグフィルター、デバッグモード）

**成果物**:
```bash
python build_tags.py
# → txt/zakki/tag/timeline.html (9セクション)
# → txt/zakki/tag/music.html (8セクション)
```

**実装完了**: 2025-12-21

### Phase 2: 高度な機能 ✅ 完了

**目標**: 統計情報とソート機能を追加

- ✅ 関連度による並べ替え（昇順・降順）
- ✅ タグ統計情報の拡張
  - 記事数
  - 最新/最古日付
  - **平均関連度（NEW！）**
- ✅ 複数ソート順に対応（コマンドライン引数）
  - `date-desc`: 日付降順（デフォルト）
  - `date-asc`: 日付昇順
  - `relevance-desc`: 関連度降順
  - `relevance-asc`: 関連度昇順
- ✅ `tag_main.html`（タグ一覧ページ）の自動生成
  - 全タグの件数・日付範囲・平均関連度を表示
  - 件数順でソート

**成果物**:
```bash
python build_tags.py --sort-by relevance-desc
# → txt/zakki/tag/timeline.html（関連度順）
# → txt/zakki/tag/music.html（関連度順）
# → txt/zakki/tag/tag_main.html（タグ一覧）
```

**実装完了**: 2025-12-21

### Phase 3: 設定ファイル対応

**目標**: コマンドライン引数の代替として設定ファイルをサポートし、保守性を向上

**実装完了日**: 2025-12-22

**実装スコープ（Phase 3.1）**:
- [x] 設定ファイル（YAML/JSON）のサポート
  - YAML形式とJSON形式の両方に対応
  - `scripts/build_tags_config.yaml` または `.json` を自動検出
  - `--config` オプションで任意のパスを指定可能
  - コマンドライン引数 > 設定ファイル > デフォルト値 の優先順位
- [x] **txt_main.html の自動更新機能** 🆕
  - タグリスト（`#taglist`）を自動更新
  - 設定ファイルでソート順を指定可能
  - 他の部分は一切変更せず、インデントを保持

**設定可能な項目**:
- `zakki_root`: zakki ディレクトリのパス
- `output_dir`: 出力先ディレクトリ
- `default_sort`: デフォルトのソート方法
- `txt_main_path`: txt_main.html のパス 🆕
- `update_txt_main`: txt_main.html を更新するか（デフォルト: true） 🆕
- `txt_main_tag_sort`: タグリストのソート順（デフォルト: count-desc） 🆕
- `tags`: タグ別の個別設定
  - `sort`: タグ固有のソート方法
  - `description`: タグの説明文（ページに表示される）

**成果物**:
```bash
# 設定ファイルを使用（自動検出）
python build_tags.py

# 設定ファイルを明示的に指定
python build_tags.py --config build_tags_config.yaml

# 設定ファイルとコマンドライン引数を組み合わせ（コマンドラインが優先）
python build_tags.py --debug  # 設定ファイルの他の設定は有効
```

**txt_main.html 更新機能**:
- スクリプト実行時に `txt/txt_main.html` の `#taglist` を自動更新
- 正規表現で該当部分のみを置換（他の部分のインデントは保持）
- ソート順を設定可能（件数順/名前順）
- 設定で無効化も可能（`update_txt_main: false`）

**実装詳細**:
- 設定ファイルは `load_config()` 関数で読み込み
- 自動検出順序: `build_tags_config.yaml` → `build_tags_config.json`
- タグ別設定により、タグごとに異なるソート順や説明文を設定可能
- サンプルファイル:
  - `build_tags_config.yaml.example`

**将来の拡張（Phase 3.2以降）**:
- [ ] セクションの省略表示機能（build_monthと同様）
- [ ] 進捗表示の改善（プログレスバー）
- [ ] 一括生成・差分更新（インクリメンタルビルド）
- [ ] タグの階層構造対応（例: `music:rock`, `music:pop`）

## build_month.py との比較

### 類似点

| 機能 | build_month.py | build_tags.py |
|------|----------------|---------------|
| **入力** | days/*.html | days/*.html |
| **処理** | 記事を読み込み | 記事を読み込み |
| **出力** | 静的HTML | 静的HTML |
| **BeautifulSoup** | 使用 | 使用 |
| **ディレクトリ走査** | 年月構造 | 年月構造 |
| **テンプレート** | 月別ページ | タグページ |

### 相違点

| 項目 | build_month.py | build_tags.py |
|------|----------------|---------------|
| **グループ化** | 月単位 | タグ単位 |
| **抽出対象** | `<article>` 全体 | `data-tags` 付きセクション |
| **省略処理** | 高度な文字数制御 | セクション単位で完全表示 |
| **ナビゲーション** | 前後月リンク | タグ一覧リンク |
| **統計** | 記事数のみ | 記事数・日付範囲・関連度 |
| **出力先** | `YYYY/MM/` | `tag/` |

## 実行結果の例

### 現在の統計（v1.0.0時点）

```
Files processed: 22
Sections found: 17
Unique tags: 2

Generated pages:
- timeline.html (9 sections)
  - 最新: 2025-12-08
  - 最古: 2024-12-13
  
- music.html (8 sections)
  - 最新: 2025-09-02
  - 最古: 2025-01-01
```

### コマンド実行例

### 1. 初回実行（全タグ生成）

```bash
cd scripts
python build_tags.py

# 出力:
# ============================================================
# タグページ自動生成スクリプト
# ============================================================
# Scanning zakki directory: ..\txt\zakki
# 
# Scan complete:
#   Files processed: 22
#   Sections found: 17
#   Unique tags: 2
# 
# Generating 2 tag page(s)...
# 
# Generating: music.html (8 sections)
#   OK Saved to: ..\txt\zakki\tag\music.html
# Generating: timeline.html (9 sections)
#   OK Saved to: ..\txt\zakki\tag\timeline.html
# 
# Successfully generated 2 tag page(s)
# 
# You can now open the generated tag pages in your browser!
```

### 2. 特定のタグのみ生成

```bash
python build_tags.py timeline music

# 出力:
# Scanning zakki directory: ..\txt\zakki
# 
# Scan complete:
#   Files processed: 22
#   Sections found: 17
#   Unique tags: 2
# 
# Generating 2 tag page(s)...
# 
# Generating: music.html (8 sections)
#   OK Saved to: ..\txt\zakki\tag\music.html
# Generating: timeline.html (9 sections)
#   OK Saved to: ..\txt\zakki\tag\timeline.html
# 
# Successfully generated 2 tag page(s)
```

### 3. 関連度降順でソート（v1.1.0〜）

```bash
python build_tags.py --sort-by relevance-desc

# 出力:
# ============================================================
# タグページ自動生成スクリプト
# ============================================================
# Scanning zakki directory: ..\txt\zakki
# 
# Scan complete:
#   Files processed: 22
#   Sections found: 17
#   Unique tags: 2
# 
# Generating 2 tag page(s)...
# Sort method: relevance-desc
# 
# Generating: music.html (8 sections)
#   OK Saved to: ..\txt\zakki\tag\music.html
# Generating: timeline.html (9 sections)
#   OK Saved to: ..\txt\zakki\tag\timeline.html
# 
# Successfully generated 2 tag page(s)
# 
# OK Generated tag_main.html: ..\txt\zakki\tag\tag_main.html
```

### 3. 関連度降順でソート（v1.1.0〜）

```bash
python build_tags.py --sort-by relevance-desc

# 出力:
# ============================================================
# タグページ自動生成スクリプト
# ============================================================
# Scanning zakki directory: ..\txt\zakki
# 
# Scan complete:
#   Files processed: 22
#   Sections found: 17
#   Unique tags: 2
# 
# Generating 2 tag page(s)...
# Sort method: relevance-desc
# 
# Generating: music.html (8 sections)
#   OK Saved to: ..\txt\zakki\tag\music.html
# Generating: timeline.html (9 sections)
#   OK Saved to: ..\txt\zakki\tag\timeline.html
# 
# Successfully generated 2 tag page(s)
# 
# OK Generated tag_main.html: ..\txt\zakki\tag\tag_main.html
```

### 4. デバッグモード

```bash
python build_tags.py timeline --sort-by date-asc --debug

# 出力:
# ============================================================
# タグページ自動生成スクリプト
# ============================================================
# Scanning zakki directory: ..\txt\zakki
# 
# Processing 2024-07...
#   Reading: 2024-07-12.html
#   ...
# 
# Processing 2024-12...
#   Reading: 2024-12-13.html
#     Found section with tags: ['timeline']
#     Found section with tags: ['timeline']
# ...
# 
# Scan complete:
#   Files processed: 22
#   Sections found: 17
#   Unique tags: 2
# 
# Generating 1 tag page(s)...
# Sort method: date-asc
# 
# Generating: timeline.html (9 sections)
#   Generating HTML for tag: timeline
#     Total sections: 9
#     Date range: 2025-12-08 to 2024-12-13
#     Average relevance: 98.9%
#     Sort method: date-asc
#   OK Saved to: ..\txt\zakki\tag\timeline.html
# 
# Successfully generated 1 tag page(s)
```

## データ構造

### タグデータの内部表現

```python
tags_data = {
    "timeline": [
        {
            "date": "2025-12-20",
            "year": "2025",
            "month": "12",
            "section_html": "<section data-tags=\"timeline=100\">...</section>",
            "relevance": 100,
            "file_path": "txt/zakki/2025/12/days/2025-12-20.html"
        },
        {
            "date": "2024-12-13",
            "year": "2024",
            "month": "12",
            "section_html": "<section data-tags=\"timeline=100\">...</section>",
            "relevance": 100,
            "file_path": "txt/zakki/2024/12/days/2024-12-13.html"
        },
        ...
    ],
    "music": [
        ...
    ]
}
```

## セキュリティとバリデーション

### data-tags パース時の検証

1. **タグ名のバリデーション**
   - 許可文字: `a-zA-Z0-9_-`（日本語も許可予定）
   - 最大長: 50文字
   
2. **関連度のバリデーション**
   - 範囲: 0〜100
   - 無効な値はデフォルト100にフォールバック

3. **HTMLのサニタイゼーション**
   - BeautifulSoupで適切にパース
   - 危険なスクリプトは含まれないことを想定

## 注意事項

### 既存ファイルの上書き

このスクリプトは既存のタグページHTMLファイルを上書きします。
初回実行前にバックアップを取ることをお勧めします。

### 前提条件

- 日別HTMLファイルに `<section data-tags="...">` が含まれている必要があります
- タグ名は `data-tags="tagname=relevance"` または `data-tags="tagname"` の形式
- 複数タグはカンマ区切り: `data-tags="tag1=100,tag2=80"`
- BeautifulSoup4がインストールされている必要があります: `pip install beautifulsoup4`

### tag-loader.js との共存

- 静的HTML版とJavaScript版は**並行して動作可能**
- 移行期間中は両方を保持し、段階的に静的版へ移行
- JavaScript版はフォールバックとして残すことも可能

### 既知の制限事項（v2.0.0）

- セクションの省略表示は未実装（全文表示）
- 差分更新は未実装（毎回全ファイル処理）
- 進捗表示の改善は未実装（シンプルなテキスト出力のみ）

これらの制限はPhase 3.2以降で順次実装予定です。

## 今後の拡張予定

### Phase 4: 高度な機能

- [ ] タグのエイリアス機能（例: `music` → `音楽`）
- [ ] タグの階層構造（例: `music:rock`, `music:jazz`）
- [ ] タグページのページネーション（大量の記事がある場合）
- [ ] RSSフィード生成（タグごと）
- [ ] JSON API生成（外部アプリケーション連携用）

### Phase 5: ビルドシステム統合

- [ ] `build.ps1` / `build.sh` への統合
- [ ] 月別ページとタグページの同時生成
- [ ] Git フックとの連携（コミット時に自動生成）
- [ ] GitHub Actions での自動ビルド

## トラブルシューティング

### `No data-tags found` と表示される

→ 日別HTMLに `data-tags` 属性が含まれているか確認してください

### タグページが空になる

→ 該当するタグが実際に存在するか、タグ名のスペルを確認してください

### 文字化けする

→ HTMLファイルがUTF-8エンコーディングで保存されているか確認してください

### txt_main.html が更新されない

→ 設定ファイルで `update_txt_main: false` になっていないか確認してください  
→ `txt_main_path` のパスが正しいか確認してください  
→ `#taglist` 要素が txt_main.html に存在するか確認してください

### txt_main.html のインデントが崩れる

→ v2.0.0 以降では正規表現による部分更新を使用しているため、この問題は解決されています  
→ 古いバージョンを使用している場合は最新版に更新してください

### 生成に時間がかかる

→ `--debug` オプションなしで実行してください。または特定のタグのみ指定してください。

## 参考資料

- [build_month.py](./build_month.py) - 月別ページ生成スクリプト（本スクリプトの設計基盤）
- [BUILD_MONTH_README.md](./BUILD_MONTH_README.md) - 月別ページ生成の詳細ドキュメント
- [tag-loader.js](../txt/zakki/tag/tag-loader.js) - 現在のJavaScript実装

## ライセンス

このスクリプトは個人利用を目的としています。

## 更新履歴

### v1.1.0 (2025-12-21) ✅ リリース済み

**Phase 2: 統計情報と拡張機能**
- ✅ 関連度ソート機能（`--sort-by`オプション）
  - `date-desc`: 日付降順（デフォルト）
  - `date-asc`: 日付昇順
  - `relevance-desc`: 関連度降順
  - `relevance-asc`: 関連度昇順
- ✅ 平均関連度の統計情報
- ✅ `tag_main.html`（タグ一覧ページ）自動生成
  - 全タグの件数・日付範囲・平均関連度を表示
  - 件数順でソート

**新機能の使用例**:
```bash
python build_tags.py --sort-by relevance-desc
```

### v1.0.0 (2025-12-21) ✅ リリース済み

**Phase 1: 基本機能の実装**
- ✅ data-tags属性の自動抽出
- ✅ タグページの静的HTML生成
- ✅ 日付順ソート（降順: 新しい順）
- ✅ タグ統計情報の表示（件数、最新/最古日付）
- ✅ コマンドライン引数対応（タグフィルター、デバッグモード）
- ✅ Windows環境対応（文字エンコーディング修正）

**実装済み機能**:
- 全日別HTMLの自動走査（2024〜2026）
- `data-tags="tag1=100,tag2=80"` 形式のパース
- イコールなしの場合デフォルト100%
- JSON形式のサポート（将来の拡張用）
- エラーハンドリングと詳細なログ出力

**生成結果**:
- Files processed: 22
- Sections found: 17
- Unique tags: 2 (timeline, music)

### v2.0.0 (2025-12-22) ✅

**Phase 3.1: 設定ファイル対応 & txt_main.html 自動更新**
- ✅ YAML/JSON形式の設定ファイルサポート
- ✅ 設定ファイルの自動検出機能
- ✅ `--config` オプションで任意のパスを指定可能
- ✅ タグ別設定（ソート順、説明文）
- ✅ 優先順位: コマンドライン > 設定ファイル > デフォルト
- ✅ サンプル設定ファイル（`.example`）を提供
- ✅ **txt_main.html のタグリスト自動更新機能** 🆕

**変更内容**:
- `load_config()` 関数を追加
- タグ説明文のHTML表示機能（個別ページと一覧ページ両方）
- PyYAMLをオプショナル依存として追加
- プロジェクトルートからの実行をサポート
- `update_txt_main_taglist()` 関数を追加（txt_main.html の `#taglist` を更新）
- 正規表現による部分更新（他の部分のインデントを保持）
- タグリストのソート順を設定可能（件数順/名前順）

**txt_main.html 自動更新**:
- スクリプト実行時に `txt/txt_main.html` の `#taglist` を自動更新
- 設定ファイルで有効/無効を切り替え可能（デフォルト: 有効）
- ソート順: `count-desc`（件数降順）, `count-asc`, `name-asc`, `name-desc`
- 正規表現で該当部分のみを置換（他の部分は一切変更しない）

**Phase 3.2以降の予定**:
- [ ] 差分更新（インクリメンタルビルド）
- [ ] 進捗表示の改善
- [ ] セクションの省略表示機能

